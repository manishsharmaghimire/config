spring:
  application:
    name: account

  datasource:
    url: jdbc:mysql://mysql-account:3306/accountdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 5
      max-lifetime: 300000
      connection-test-query: SELECT 1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
    virtual-host: /

server:
  port: 8084
  servlet:
    context-path: /

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
      show-components: always
  health:
    probes:
      enabled: true
  validate-group-membership: false   # disables invalid group validation
  health:
    group:
      readiness:
        include: ["db", "rabbit", "diskSpace"]  # health checks considered for readiness
      liveness:
        include: ["diskSpace"]                  # health checks considered for liveness

accounts:
  message: "Welcome to abc accounts related production APIs"
  contactDetails:
    name: "Reine Aishwarya - Product Owner"
    email: "aishwarya@abc.com"
  onCallSupport:
    - "(453) 392-4829"
    - "(236) 203-0384"

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    com.abc: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
