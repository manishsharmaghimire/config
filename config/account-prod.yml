spring:
  application:
    name: account

  datasource:
    url: jdbc:mysql://mysql-account:3306/accountdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: "*"
  

# -------------------- Info / Contact --------------------
info:
  contact:
    name: Manish Sharma
    email: manish@examplesss.com
    phone: "+977-98XXXXXXXXy"
